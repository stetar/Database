using Microsoft.Xna.Framework;

namespace Working_title.MapGenerator
{
    public class Enemy : BuildObject
    {
        public Enemy(Vector2 position, Size size) :
            base(position, size)
        {
        }
    }
}